RubyAU Styleguide
=================

Installation
------------

    $ npm install

Updating the CSS
----------------

The project uses cssnext so needs to be compiled if you change the CSS.

    $ npm run build


